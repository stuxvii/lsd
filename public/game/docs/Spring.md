The Spring will set the Velocity of any PhysicsBody3D that enters its LHitbox

to the amount set in the Force export variable which is then multiplied by the

direction of the Spring itself. This action will override any pre-existing velocity.